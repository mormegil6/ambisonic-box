# Upstream contributions

Bugs found while building this stack are reported to, and fixed in, the projects they belong to rather than only patched here. This page is the register: what has been sent where, what is still open, and what is prepared but not sent.

It exists for a practical reason as well as a tidy one. Work in this repository is spread across two forked submodules (the player and the media server), three patched npm packages, a vendored plugin inside the player, and a browser bug or two, and without a single list it is genuinely easy to lose track of what has already been submitted and start drafting it twice.

Status is as of 2026-08-28. Nothing here is auto-generated, so re-check the links before quoting any of it.

## Merged

| Project | PR or issue | What |
|---|---|---|
| EnvelopSound/Earshot | [#53](https://github.com/EnvelopSound/Earshot/pull/53) | Align track starts at the relay so DASH output carries no video edit list. This is the server-side half of the A/V desync investigation; the client-side half is the browser divergence catalogued in [thomasdeppisch/hoast360#33](https://github.com/thomasdeppisch/hoast360/issues/33) and reproduced at [mse-edit-list-repro](https://github.com/mormegil6/mse-edit-list-repro) |
| EnvelopSound/Earshot | [#54](https://github.com/EnvelopSound/Earshot/pull/54) | Enable libvpx in ffmpeg so VP9 is actually available |
| EnvelopSound/Earshot | [#55](https://github.com/EnvelopSound/Earshot/pull/55) | Clear the DASH directory contents instead of `rm -rf` on the `/opt/data` mount |
| EnvelopSound/Earshot | [#56](https://github.com/EnvelopSound/Earshot/pull/56) | nginx: emit relative redirects so a mapped host port survives directory redirects |
| EnvelopSound/Earshot | [#57](https://github.com/EnvelopSound/Earshot/pull/57) | Allow building without `--enable-nonfree` for a redistributable image |
| EnvelopSound/Earshot | [#61](https://github.com/EnvelopSound/Earshot/pull/61) | Raise the yarn network timeout so slower hosts can build the webtools image |
| EnvelopSound/Earshot | [#62](https://github.com/EnvelopSound/Earshot/pull/62) | CI: make the workflows run again (actions v4, Compose v2, failure diagnostics). Restored the pipeline but left `test` red on a pre-existing, unrelated mismatch: the bundled test source is audio-only and the shipped default (`-c:v copy`) needs video. See [#63](https://github.com/EnvelopSound/Earshot/pull/63) |
| EnvelopSound/Earshot | [#63](https://github.com/EnvelopSound/Earshot/pull/63) | Test: push a real video track, matching the default `-c:v copy` path, so CI's `test` job goes green again after [#62](https://github.com/EnvelopSound/Earshot/pull/62) |
| EnvelopSound/Earshot | [#64](https://github.com/EnvelopSound/Earshot/pull/64) | Resume the AudioContext from the gain slider's `onChange` (a real user gesture) instead of a `window` click listener. Supersedes [#26](https://github.com/EnvelopSound/Earshot/pull/26) (closed), which diagnosed the bug correctly in 2021 but leaked a listener on every re-render. Closes [#13](https://github.com/EnvelopSound/Earshot/issues/13) ("No audio playback in Chrome"), open since 2021 and previously misdiagnosed in that thread as an HTTPS/CDN problem |
| EnvelopSound/Earshot | [#58](https://github.com/EnvelopSound/Earshot/pull/58) | Floor DASH `suggestedPresentationDelay` to avoid live-join gap-jumps |
| EnvelopSound/Earshot | [#59](https://github.com/EnvelopSound/Earshot/pull/59) | Decouple the DASH manifest filename from the RTMP stream key |
| EnvelopSound/Earshot | [#60](https://github.com/EnvelopSound/Earshot/pull/60) | Raise RTMP `max_message` so 4K keyframes are not dropped |
| EnvelopSound/Earshot | [#70](https://github.com/EnvelopSound/Earshot/pull/70) | webtools: bump 13 dependencies to patched versions already inside their declared ranges (real CVEs/GHSAs per package, not a routine version chase). Caught two npm-registry-deprecated release traps (`lodash`/`lodash.template` 4.18.0) before they shipped as "fixes". Open alert count: 180 -> 114 across this PR and the 11 Dependabot PRs merged the same day. Five packages from the same alert sweep deliberately excluded - each cascades into further version conflicts and needs its own PR |
| EnvelopSound/Earshot | [#71](https://github.com/EnvelopSound/Earshot/pull/71) | `.gitattributes` had no `eol` rule for shell scripts, so a Windows checkout with the common `core.autocrlf=true` setting silently CRLF-converts `entrypoint.sh`, and Docker bakes the corrupted script into the image via `COPY`, crashing with `standard_init_linux.go:219: exec user process caused: no such file or directory`. Forces LF for all `*.sh` regardless of checkout platform. Closes [#28](https://github.com/EnvelopSound/Earshot/issues/28), open since 2021 |
| EnvelopSound/Earshot | [#72](https://github.com/EnvelopSound/Earshot/pull/72) | webtools: patch the 29 vulnerable packages Dependabot cannot auto-fix - transitive deps pinned by their requesters' ranges, fixed via 18 in-range lockfile bumps (a single in-range `express` 4.18.2 -> 4.22.2 carries five of its own sub-dependencies), 10 scoped yarn `resolutions`, and the one direct dep with an alert (`xml2js`). Open alert count 114 -> 58. The fix that matters beyond hygiene: `path-to-regexp`'s ReDoS was reachable client-side (react-router bundles it into the served app). Deliberately excluded and named in the PR: 23 packages needing major-version overrides into react-scripts' own tooling, and 3 with no patched version at all |
| EnvelopSound/Earshot | [#73](https://github.com/EnvelopSound/Earshot/pull/73) | webtools: the one package from [#72](https://github.com/EnvelopSound/Earshot/pull/72)'s excluded "needs-care" bucket worth fixing on its own - `@babel/runtime` ships its helpers into whatever the transpiler emits, so its ReDoS advisory (inefficient RegExp complexity transpiling named capturing groups) isn't purely build-time like the rest of that bucket. `babel-preset-react-app` (react-scripts' own preset) exact-pins it to `7.9.0`, so the fix needed a `resolutions` override rather than an in-range bump |
| EnvelopSound/Earshot | [#74](https://github.com/EnvelopSound/Earshot/pull/74) | webtools: a second pass over [#72](https://github.com/EnvelopSound/Earshot/pull/72)'s "needs-care" bucket, separating genuinely low-risk same-major deltas (`braces`, `minimist`, `semver` across its 5/6/7 lines, `browserslist`, `@babel/core`) from the cross-major jumps that stay excluded. Caught two real problems only a full build surfaces: the CVE's bare-minimum `browserslist` patch left `@babel/core` unable to find a function it needs, and the newer `browserslist` pulled in a transitive `node-releases` release that requires Node >=18 on a stage still running Node 12 - both required going past the minimum patched version, not just to it, to find a version that actually builds |
| EnvelopSound/Earshot | [#75](https://github.com/EnvelopSound/Earshot/pull/75) | webtools: closes three old-line stragglers a GitHub weekly digest email caught after [#74](https://github.com/EnvelopSound/Earshot/pull/74) - packages already fixed on their main line, but with a second, older stanza nobody checked. `braces`' CVE turned out to be a blanket range with no major carve-out ([#74](https://github.com/EnvelopSound/Earshot/pull/74) wrongly assumed the 2.3.2 line, pulled in by jest-only `chokidar`/`micromatch`, was a separate, unaffected major); `qs`'s old `request`-pinned line was stuck below the advisory range while its `express`/`body-parser` line was already fixed in [#72](https://github.com/EnvelopSound/Earshot/pull/72); `minimist`'s 0.0.8 line, exact-pinned by `mkdirp`, turned out to be dead weight entirely - required only by mkdirp's CLI wrapper, never its library entry point this project actually calls. All three forced to versions already proven safe elsewhere in the same lockfile. `uuid` and `yargs-parser` have the same multi-stanza shape but were checked and confirmed genuinely blocked, not assumed |
| EnvelopSound/Earshot | [#76](https://github.com/EnvelopSound/Earshot/pull/76) | webtools: traced every remaining alert's actual requester chain against react-scripts' own `build.js`/`start.js` require lists instead of treating "core build tooling" as reason enough to leave a package alone. Landed 8: `webpack-dev-middleware`/`http-proxy-middleware`/`node-forge` (all reachable only through `webpack-dev-server`, itself required only by `start.js` - this project's Docker image never runs it), `micromatch`/`node-notifier` (jest-only, and CI never runs jest here), `flatted` (eslint's interactive `--init` wizard, a path `yarn lint` never loads), `tough-cookie` (absent from dashjs's actual shipped bundle, confirmed by grep), and `tmp` - unblocked by bumping the build stage off EOL `node:12.22.1-alpine3.12` to `node:24-alpine3.24`, the exact change already proven byte-identical on the maintainer's own fork, re-verified here. Only `webpack-dev-server` itself stays excluded: it's react-scripts' own direct dependency, and its `verifyPackageTree` preflight check hard-fails on any version mismatch there regardless of reachability - confirmed by trying it |
| EnvelopSound/Earshot | [#77](https://github.com/EnvelopSound/Earshot/pull/77) | webtools: two more packages [#74](https://github.com/EnvelopSound/Earshot/pull/74)'s original triage left behind turned out to share [#76](https://github.com/EnvelopSound/Earshot/pull/76)'s shape. `node-notifier` (fixed in [#76](https://github.com/EnvelopSound/Earshot/pull/76), jest-only) is one of `uuid`'s four requesters; the other three (`sockjs`, `webpack-log`, both via `webpack-dev-server`) trace to the same dead chain [#76](https://github.com/EnvelopSound/Earshot/pull/76) already proved, and the fourth (`dashjs`'s own `request` dependency) checked out clean by grepping the actual shipped bundle for uuid-formatted strings - the only matches are dash.js's own hardcoded ClearKey DRM key-system identifiers, not the package. `yargs-parser`'s vulnerable stanza is required only by `yargs@12.0.5`, itself required only by `webpack-dev-server` - consolidated to the same 13.1.2 already used elsewhere via jest. Content hash of the shipped bundle unchanged across [#74](https://github.com/EnvelopSound/Earshot/pull/74)-[#77](https://github.com/EnvelopSound/Earshot/pull/77), confirming none of this chain has ever touched what actually ships |
| EnvelopSound/Earshot | [#78](https://github.com/EnvelopSound/Earshot/pull/78) | webtools: `is-svg`, `nth-check`, `svgo` forced past what their real requesters declare, verified unreachable via three separate paths rather than the one originally assumed. `@svgr/webpack`'s SVG loader never runs (zero `.svg` imports anywhere in the app). `postcss-svgo` genuinely is active on every production build (wired into `cssnano-preset-default`, which react-scripts always runs) and walks every `url()` in the bundled CSS, but the app's only two CSS files contain none, so its `svgo`/`is-svg` call is never reached. `css-select@1.2.0`, pulled in by `html-webpack-plugin`'s build-error formatter (`pretty-error` -> `renderkid`) and present on every build regardless of app content, never calls `nth-check` because `renderkid`'s own bundled selector rules never use an `nth-child`/`nth-of-type`-family pseudo-class. That last path mattered most to get right: `nth-check@2.1.1`'s CJS export is no longer directly callable the way `css-select@1.2.0` expects it to be (confirmed empirically, including inside `node:24-alpine3.24` itself, the Dockerfile's own build image) - so if that path is ever exercised, it crashes outright. Copilot's review caught this gap directly: the PR's first version of this reasoning only covered the `.svg`-import path, and its own follow-up claim that `nth-check@2.1.1` was still callable turned out to be incorrect, verified and rebutted with the same reproduction |
| EnvelopSound/Earshot | [#79](https://github.com/EnvelopSound/Earshot/pull/79) | webtools: five more patched versions, from a fan-out investigation across the packages remaining after [#78](https://github.com/EnvelopSound/Earshot/pull/78) (20 alerts) - `tmp` 0.2.4 -> 0.2.7 and `flatted` 3.4.0 -> 3.4.4 raise resolutions already forced by earlier PRs to clear newer patched-version alerts; `fast-xml-parser` (new entry, 5.7.0) is a transitive dependency [#78](https://github.com/EnvelopSound/Earshot/pull/78)'s own `is-svg` bump introduced, triple-unreachable (`is-svg`'s `isSvg()` is never called, requiring `is-svg` only binds classes without executing them, and even a reached `isSvg()` could never hit the actual vulnerable function since `is-svg` only imports `XMLParser`/`XMLValidator`, never `XMLBuilder`); `react-dev-utils` (new entry, 11.0.4, a real cross-major override against react-scripts' declared `^10.2.1`) corrects a stale exclusion verdict from before the reachability-tracing methodology existed - its vulnerable function is `start.js`-only and this project's Docker build never runs `start.js`; `serialize-javascript` (new entry, 7.0.3) is the one package here already known to be unconditionally reachable (`terser-webpack-plugin`'s core minification dispatch, including a call site feeding `new Function(...)`), verified safe by diffing actual bundle bytes - not just content hashes - between an unmodified baseline and the bumped build, byte-for-byte identical. `flatted`'s own prior commit message ([#76](https://github.com/EnvelopSound/Earshot/pull/76)) claimed reachability via eslint's `--init` wizard through inquirer; that chain does not exist, the real one is eslint's `--cache` option, never enabled here - the conclusion held, the stated reason didn't. `postcss` (5 alerts) was investigated and deliberately excluded: every one of its 84 requesters pins `^7.x`, no alert has ever been patched within 7.x, and forcing it to 8.x was tested directly (not just reasoned about) with a genuine blanket override - it fails immediately with a real `TypeError`, confirming postcss 8's plugin-API break actually hits this react-scripts@3.4.1-era toolchain rather than being a theoretical risk |
| EnvelopSound/Earshot | [#80](https://github.com/EnvelopSound/Earshot/pull/80) | webtools: two alerts (one CRITICAL) that [#79](https://github.com/EnvelopSound/Earshot/pull/79) introduced within a minute of merging - the same self-inflicted-follow-up shape as `fast-xml-parser` was for [#78](https://github.com/EnvelopSound/Earshot/pull/78). `immer` (new entry, 9.0.6, prototype pollution) rode in as `react-dev-utils@11.0.4`'s own dependency; traced to its one real call site (`react-scripts/scripts/utils/verifyTypeScriptSetup.js`, called unconditionally from `build.js` but returning immediately when no `tsconfig.json` exists and zero `.ts`/`.tsx` files are found - both true here) before it ever reaches immer, then forced to the patched version anyway for a clean close rather than another exclusion note. Scoped to `**/react-dev-utils/immer` rather than a blanket override, per Copilot's review, matching the file's existing `**/react-dev-utils/cross-spawn` pattern - react-dev-utils is immer's only requester today, so the scoped form closes the alert without also silently forcing 9.0.6 onto some unrelated future requester. `serialize-javascript` 7.0.3 -> 7.1.0 closes a newly-published DoS advisory (`<7.0.5`) that [#79](https://github.com/EnvelopSound/Earshot/pull/79)'s fix sat inside; re-verified with the same byte-diff rigor as [#79](https://github.com/EnvelopSound/Earshot/pull/79) since this is the one package in the chain already known to be actively reachable |
| EnvelopSound/Earshot | [#81](https://github.com/EnvelopSound/Earshot/pull/81) | Dockerfile: build ffmpeg from the upstream `n7.1` release tag instead of `EnvelopSound/ffmpeg@earshot-v0.1`, a six-year-old fork reporting `4.3.git ... 2020`. The fork existed only for PCE-aware 16-channel AAC, and that no longer needs a fork: decode of 16-ch PCE AAC is upstream, and encode is native at n7.1 (`aac_pce_configs[]` carries `HEXADECAGONAL` there - it was dropped from master around `c92c6cbf`, a separate ffmpeg regression, which is why n7.1 is the target rather than a newer release). The move closes ~6 years of CVE/hardening on the demuxer/decoder paths that meet contribution bytes (`aacdec`, `flvdec`, `mpegts`) and unlocks Opus-in-fMP4 DASH, which the 2020 muxer could not emit - fixing the mixed fMP4-video/WebM-audio manifest that FFmpeg-based players (found by Keven Ma testing with Twinkle/ffplay) cannot demux cleanly. The one carried patch, the DASH suggestedPresentationDelay-floor sed on `dashenc.c`, applies to n7.1 verbatim. Verified on real amd64 AND native arm64 (Raspberry Pi 4) builds: 16-ch AAC encode/decode, 16-ch Opus, Opus-in-fMP4, SPD floor `PT30S`, full pipeline PASS, and browser playback on Chrome + Firefox |
| EnvelopSound/Earshot | [#82](https://github.com/EnvelopSound/Earshot/pull/82) | Dockerfile: fetch ffmpeg from ffmpeg.org's release tarball (`ffmpeg-7.1.tar.xz`) and verify it against a pinned SHA-256 before extracting or compiling, instead of GitHub's `/archive/refs/tags/` URL. Follow-up to [#81](https://github.com/EnvelopSound/Earshot/pull/81), and the reason the pin is worth anything: GitHub archive tarballs are generated on demand and their bytes have changed before, so a checksum against that URL pins nothing; the release tarball is a fixed, signed artifact. The pinned hash was checked against FFmpeg's own release signing key (`FCF9 86EA 15E6 E293 A564 4F10 B432 2F04 D676 58D8`) before being hardcoded, rather than trusted from one download. `xz` joins the build deps and the extract dir becomes lowercase `ffmpeg-7.1`. Also records the 16-channel AAC ceiling at the version line: releases 7.0 through 8.1 carry `HEXADECAGONAL` in `aac_pce_configs[]`, 9.0 onward does not, and losing it breaks the RTMP contribution leg at RUNTIME while the image still builds clean. Verified on amd64 and native arm64 (Raspberry Pi 4): checksum passes, image builds, SPD-floor patch still applies, 16-ch hexadecagonal AAC still encodes; and a deliberately corrupted pin aborts the build at `sha256sum -c` before extraction, producing no image |
| EnvelopSound/Earshot | [#83](https://github.com/EnvelopSound/Earshot/pull/83) | Records why the AudioContext `resume()` from [#64](https://github.com/EnvelopSound/Earshot/pull/64) is needed, with the measurement behind it (gain alone 0.0 RMS, plus unmute 0.0, plus resume 5.6e-4). Comment only: a bare `resume()` reads as boilerplate and invites removal |
| EnvelopSound/Earshot | [#84](https://github.com/EnvelopSound/Earshot/pull/84) | `/nginxInfo` returns only `ffmpegFlags`, but the manifest is written to `${DASH_NAME}.mpd`, a name that need not match the publish name, so a client cannot work out which URL to request. Adds the field to both config variants Review also caught that the endpoint declared no content type, so `default_type application/json;` went in with it. |
| FFmpeg/FFmpeg | [24218](https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24218) | `aacenc` could encode no channel layout above 8 since `c92c6cbf19` removed the last `aac_pce_configs[]` entries above `OCTAGONAL`, taking away the 16-channel carrier third-order Ambisonics needs on the AAC contribution leg. Filed 2026-08-20 as a capability loss rather than a revert request, since the removal was deliberate: the ask was a PCE for N discrete single-channel elements with no positional claim. Closed 2026-08-23, fixed by `ed923a7a89` (PCE configs for the height layouts) and `f35acb72ac` (ambisonic layouts at 4, 9 and 16 channels). The 9-channel entry carries this project's own contribution: the reorder map `{2,5,6,0,1,7,8,3,4}`, measured here and adopted verbatim. 25 channels was not added, so 4th order still has no AAC route. No release carries any of this, 9.0.1 of 2026-08-12 predates it, so `earshot` stays pinned to FFmpeg 7.1 |

## Open

| Project | PR | What |
|---|---|---|
| thomasdeppisch/videojs-xr | [#28](https://github.com/thomasdeppisch/videojs-xr/pull/28) | Mobile orientation controls, renderer sizing and a three.js deprecation: `Math.clamp` does not exist, `rotateLeft`/`rotateUp` are not exposed on the OrbitControls instance (which kills the render loop on any device with an orientation sensor), `Quaternion.inverse()` is deprecated, and the mono renderer is never resized after a zero-size init. Also carries the projection-sphere tessellation fix (32x32 to 256x128): UV interpolation is linear across each quad while the equirect-to-sphere map is not, so at 11.25 deg per quad straight lines visibly bow, worst at the poles where the azimuth segments converge. Rendered before/after in [docs/VOD.md](VOD.md) |
| thomasdeppisch/hoast360 | [#30](https://github.com/thomasdeppisch/hoast360/pull/30) | Resume the AudioContext on the combined-MPD path, which was silent on every browser enforcing the autoplay policy |
| thomasdeppisch/hoast360 | [#31](https://github.com/thomasdeppisch/hoast360/pull/31) | `HoastLoader.concatBuffers()` reads source channel 0 for every destination in a higher-order group, so 10 of 12 third-order filter channels load the wrong decoding filter. Measured in [aac-bitrate-test/RESULTS.md](../aac-bitrate-test/RESULTS.md) |
| thomasdeppisch/hoast360 | [#32](https://github.com/thomasdeppisch/hoast360/pull/32) | Replace the Opus support probe, which tests `canPlayType('audio/ogg; codecs="opus"')` for a container this player never streams, with a real `decodeAudioData` probe that also names the Chrome field trial (`DirectOpusAudioDecoding`) behind one failure mode it distinguishes. Independent of [#30](https://github.com/thomasdeppisch/hoast360/pull/30) and [#31](https://github.com/thomasdeppisch/hoast360/pull/31) |
| thomasdeppisch/hoast360 | [#33](https://github.com/thomasdeppisch/hoast360/issues/33) | An issue rather than a patch: a DASH source whose video track carries a leading empty edit (`elst media_time = -1`) plays with the picture ahead of the sound and no error anywhere, because Chromium's MSE drops the offset its own `<video src>` path applies. Offers the fork's `SegmentAudioFeed` as a player-side compensation |
| thomasdeppisch/hoast360 | [#34](https://github.com/thomasdeppisch/hoast360/pull/34) | `initialize()` requires the ambisonic order as an argument, so every embedder hardcodes what the stream already states in `AudioChannelConfiguration`. Reads it when the argument is omitted; passing one explicitly still works Review caught that it only resolved the combined-MPD layout; it now branches on `.mpd` the way `initialize()` does. |
| thomasdeppisch/hoast360 | [#35](https://github.com/thomasdeppisch/hoast360/pull/35) | No caption path exists, and wiring one from outside hits three silent failures: `addRemoteTextTrack` missing until the player is ready, cross-origin `.vtt` dropped without `crossorigin="anonymous"` and without a console error, and a track that is added but never set to `mode = 'showing'` |
| Dash-Industry-Forum/dash.js | [#5104](https://github.com/Dash-Industry-Forum/dash.js/pull/5104) | Guard three unguarded reads that crash on legitimate input: a `SegmentTimeline` that is absent or carries no `S` elements ([#3513](https://github.com/Dash-Industry-Forum/dash.js/issues/3513) and [#2708](https://github.com/Dash-Industry-Forum/dash.js/issues/2708) reported the class years ago, closed without a guard), and the ISOBMFF assumption in `BoxParser.getSamplesInfo` and the CEA-608 path, which WebM segments do not satisfy. Four unit tests, each failing on `development` without the change |

## Reported upstream (no patch attached)

| Where | What |
|---|---|
| [Chromium 537235698](https://issues.chromium.org/issues/537235698) | Chromium's MSE does not apply a video track's empty-edit (`elst`) presentation offset while Firefox does, so identical fMP4 bytes play in sync in one engine and ~1.7 s out in the other. Filed as an interoperability question. Minimal reproduction: [mse-edit-list-repro](https://github.com/mormegil6/mse-edit-list-repro) |
| [WebKit 319998](https://bugs.webkit.org/show_bug.cgi?id=319998) | The same MSE empty-edit divergence, filed against Safari/WebKit. Resolved as a duplicate of [316870](https://bugs.webkit.org/show_bug.cgi?id=316870); the fix landed in WebKit trunk 2026-07-07 (`316626@main`), but no shipping Safari carries it yet - 26.5.2 and 27.0 beta 1 both still drop the offset as measured |
| [Mozilla 2056945](https://bugzilla.mozilla.org/show_bug.cgi?id=2056945) | The mirror-image half of the same gap: Firefox already applies the empty-edit offset correctly under MSE (the common streaming case) but drops it on the plain, non-MSE `<video src>` path - filed separately since it is Gecko's own defect, not the Chromium/WebKit MSE-side one |
| [w3c/media-source#377](https://github.com/w3c/media-source/issues/377) | The same divergence raised against the MSE spec: empty-edit (`elst media_time = -1`) handling diverges across engines and from non-MSE playback in the same engine |
| [Chromium 547065816](https://issues.chromium.org/issues/547065816) | `DirectOpusAudioDecoding`, a field trial rolling out in Chrome 151, breaks every Opus decode above 2 channels in both `decodeAudioData` and MSE playback. Filed as a regression: multichannel Opus was deliberately enabled in M62 (issue 41292687) and this trial withdraws it. Brave and Edge fail identically when the feature is forced on, so it is Chromium code rather than Chrome packaging. Reproduction: [opus-multichannel-repro](https://github.com/mormegil6/opus-multichannel-repro) ([live check](https://mormegil6.github.io/opus-multichannel-repro/)). Reader-facing write-up: [CHROME-MULTICHANNEL-OPUS.md](CHROME-MULTICHANNEL-OPUS.md). Fixed upstream and marked verified in 154.0.8021.0; as of 2026-08-26 the tracker carries `Merge: 152, 153` and the assignee is merging it back, so it should land before M154 stable |
| [Shaka Packager 1627](https://github.com/shaka-project/shaka-packager/issues/1627) | `OpusSpecific::ReadWriteInternal` copies the `OpusHead` body into the `dOps` box without byte-swapping, so `PreSkip`, `InputSampleRate` and `OutputGain` come out little-endian in a box ISO BMFF requires to be big-endian. A 48 kHz stream reports `InputSampleRate` 2159738880 and Chromium's MSE refuses the segment; progressive playback is unaffected, which is likely why it survived. Reproduces from a file already in their repo, and their own committed golden `bear-320x240-vp9-opus-audio.mp4` carries the malformed box, so the test suite requires it. Two-channel, mapping family 0, i.e. nothing to do with ambisonics. Unchanged since `cf4a2447` in 2016. The read path has the mirror defect, which is why WebM-to-MP4 corrupts while MP4-to-MP4 cancels out and looks fine. Patch written and verified here against their unit tests and Chromium; PR offered in the issue, not yet opened |
| [Ambiqual #2](https://github.com/QxLabIreland/Ambiqual/issues/2) | `calculate_ambiqual` references `n_channels`, which no longer exists after a rename to `n_channels_ref`/`n_channels_deg`, so the tool raises `NameError` on the first channel and cannot score anything as published. Filed rather than patched because the right behaviour on mismatched channel counts is the maintainers' call: `min()` matches what the downstream NaN handling already assumes, but rejecting the input outright is defensible too. The pinned `numpy==1.23.5` also has no wheel for current Python. |

Considered and rejected: adding `-b:a 1024k` to the Opus encode. The argument would have been that upstream sets no audio bitrate, so libopus picks its own for a 16-channel stream. Measured in the earshot image first: the default and an explicit `1024k` both land at ~1198 kbit/s, so the flag changes nothing upstream would notice and stays a local tuning choice.

## Not upstreamable

Some patches are specific to this deployment and are carried locally on purpose. The DVR seek clamp, the segment audio feed and the pixel-ratio rendering change all live in the [hoast360 fork](https://github.com/mormegil6/hoast360) rather than upstream, either because they encode choices another deployment should not inherit or because they depend on this fork's own modernised build.

The DASH quality-levels bridge belongs in that category too, for a reason worth writing down so it is not proposed again. Upstream hoast360 depends on the maintainer's own forks of `videojs-contrib-dash` and `videojs-http-source-selector`, which already handle this: `thomasdeppisch/videojs-contrib-dash@96baa2b` ("make it work with source selector for quality levels") and `thomasdeppisch/videojs-http-source-selector@f0d032d` ("set levels.selectedIndex_ properly"), both from 2020. This fork swapped those github dependencies for published npm releases because their prepublish step pulls node-sass 4 and will not install on Node 17 or newer. That trade bought installability and cost the integration, so the bridge rebuilds it here. Upstream has no gap to fix.
